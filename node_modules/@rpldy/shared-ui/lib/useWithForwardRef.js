"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _react = require("react");

var _shared = require("@rpldy/shared");

/**
 * can be used to support forward ref that can be either a function or an object (useRef, createRef)
 * @param ref {Object | Function}
 */
var useWithForwardRef = function useWithForwardRef(ref) {
  var internalRef = (0, _react.useRef)(null);
  var setRef = (0, _react.useCallback)(function (elm) {
    if (ref) {
      if ((0, _shared.isFunction)(ref)) {
        ref(elm);
      } else {
        ref.current = elm;
      }
    }

    internalRef.current = elm;
  }, [ref]);
  return {
    ref: internalRef,
    setRef: setRef
  };
};

var _default = useWithForwardRef;
exports["default"] = _default;